import React from 'react';
import PropTypes from 'prop-types';

const NewPoll = props => {
    return (
        <div>
            NewPoll
        </div>
    );
};

NewPoll.propTypes = {
    
};

export default NewPoll;